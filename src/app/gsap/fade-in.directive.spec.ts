import { FadeInAnimationDirective } from './fade-in-animation.directive';

describe('FadeInDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
